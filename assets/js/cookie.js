function getCookieByName( name ){
    
}

function setCookieAdd( name, val, options = {} ){
    // let currentCookie = 
    // options.reset
    options = {
        reset: false,
    };
    if( options.reset == true ){

    }
}
