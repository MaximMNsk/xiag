$(document).ready(()=>{
    // document.cookie 
    alert( document.cookie );
});

function getCookieByName( name ){

}

function setCookieAdd( name, val, options = {} ){
    // let currentCookie = 
    // options.reset
    
}
